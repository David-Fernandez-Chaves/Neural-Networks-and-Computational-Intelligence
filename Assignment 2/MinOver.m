function [W,K] = MinOver(Inputs,Labels)
%   MinOver algorithm

    Bias = 0;
    p = size(Input,2);
    
    W=[zeros(1,Ndimensions),Bias];
    Wold=[ones(1,Ndimensions),Bias];
    Inputs = [Inputs;-ones(1,p)];

    while W ~= Wold,
        
        %Get the min Stability
        Ks=
        
        sample=0;

        for i=1:p
            
        end
        
        
    end


    K=0;
end

